# @package _global_

# to execute this experiment run:
# python run.py experiment=hyperparameter_search/m2_vae_protein.yaml

defaults:
  - override /datamodule: protein.yaml
  - override /model: m2_vae.yaml
  - override /callbacks: default.yaml
  - override /logger: wandb.yaml
  - override /trainer: experiment.yaml

name: "m2_vae"

model:
  enc_in_features: 10
  enc_out_features: 2
  dec_in_features: 3
  dec_out_features: 9
  reg_in_features: 9

seed: 42

logger:
  wandb:
    name: ${name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
    job_type: "hyperparameter_search"
    group: "m2_vae_protein"
    tags: 
      - ${name}
      - "protein"
      - "hyperparameter_search"
    notes: ""